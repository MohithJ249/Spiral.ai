import { Typography } from '@mui/material';

export default function ErrorPage() {
    return (
        <>
            <div>
                <Typography variant="h3">Add error page here (status 404). Check in content/Error/index.tsx file</Typography>            
            </div>
        </>
    );
}