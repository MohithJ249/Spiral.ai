import { Typography } from '@mui/material';

export default function SharedPage() {
    return (
        <>
            <div>
                <Typography variant="h3">Add shared page here. Check in content/Shared/index.tsx file</Typography>            
            </div>
        </>
    );
}